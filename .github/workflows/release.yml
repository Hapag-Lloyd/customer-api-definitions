---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  default:
    # yamllint disable-line rule:line-length
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/maven_release_callable.yml@1d69eff5b1f520d3fb3b553798c8248a8581d1d3 # 1.23.0
    secrets: inherit
